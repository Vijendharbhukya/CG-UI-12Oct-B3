let msg;
msg=<p>The actual script is an external script called common.js</p>;

function addNos(headvar,bodyvar)
{
 console.log(msg);
 const addNos=(headvar,bodyvar)=>{
 
        console.log(headvar+bodyvar);
 }
}